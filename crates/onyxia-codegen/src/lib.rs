//! WGSL shader compiler and execution graph builder for Onyxia.
//!
//! This crate takes the IR from `onyxia-onnx` and generates WGSL compute shaders,
//! producing an executable graph that `onyxia-runtime` can execute on the GPU.

pub fn placeholder() {
    todo!("Codegen implementation")
}

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        // Placeholder test
    }
}
